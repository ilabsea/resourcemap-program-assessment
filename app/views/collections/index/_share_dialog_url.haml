%div{ class: "box centered w40 rm-dialog hidden", id: "rm-share-url-dialog", style: "margin-left: -200px; margin-top: -100px;" }
  %h3 Share secret
  %p
    Copy the url below to share this site detail in predefined design template with other party
  %a#share-url-dialog{href: "", target: :_blank, style: 'color:#777777;'}
  %div{ style: "margin-left: 15px; padding-top: 10px;"}
    %button{ type: "button", value: "Close", class: "white rm-dialog-cancel" } Close
  .clear &nbsp;

:javascript
  $(function(){
    $.fn.rmDialog = function(options){
        return this.each(function(){
            $(this).show();
            dialog = $(this);
            $(this).find(".rm-dialog-cancel").on("click", function(){
                dialog.hide();
            });
        });
    }
  });
